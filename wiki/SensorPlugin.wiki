This plugin collects data on available device sensors.

*Plugin Name:* Sensor Plugin

*Class Name:* {{{org.androidanalyzer.plugins.sensor.SensorPlugin}}}

The following table defines the structure of data being inserted in the report, and the APIs or methods used to retrieve the value.

|| *Node* || *Node* || *Node* || *Value* || *Retrieval Method* ||
|| Sensor|| || || || ||
||      || Accelerometer || || _boolean_ (Y, N) || {{{SensorManager.getSensorList(TYPE_ACCELEROMETER).size()>0}}} ||
||      || Proximity|| || _boolean_ (Y, N) || {{{SensorManager.getSensorList(TYPE_PROXIMITY).size()>0}}} ||
||      || Gyroscope|| || _boolean_ (Y, N) || {{{SensorManager.getSensorList(TYPE_GYROSCOPE).size()>0}}} ||
||      || Light|| || _boolean_ (Y, N) || {{{SensorManager.getSensorList(TYPE_LIGHT).size()>0}}} ||
||      || Orientation|| || _boolean_ (Y, N) || {{{SensorManager.getSensorList(TYPE_ORIENTATION).size()>0}}} ||
||      || Pressure|| || _boolean_ (Y, N) || {{{SensorManager.getSensorList(TYPE_PRESSURE).size()>0}}} ||
||      || Temperature|| || _boolean_ (Y, N) || {{{SensorManager.getSensorList(TYPE_TEMPERATURE).size()>0}}} ||
||      || Magnetic field|| || _boolean_ (Y, N) || {{{SensorManager.getSensorList(TYPE_MAGNETIC_FIELD).size()>0}}} ||
|| || Sensor-{{{<x>}}}|| || || ||
|| || || Type || _integer_ || {{{Sensor.getType()}}} ||
|| || || Name || _string_ || {{{Sensor.getName()}}} ||
|| || || Vendor || _string_ || {{{Sensor.getVendor()}}} ||
|| || || Version || _integer_ || {{{Sensor.getVersion()}}} ||
|| || || Power || _double_ || {{{Sensor.getPower()}}} ||
|| || || Resolution || _double_ || {{{Sensor.getResolution()}}} ||
|| || || Maximum Range || _double_ || {{{Sensor.getMaximumRange()}}} ||

Note: for each present sensor a separate Sensor-{{{<x>}}} node is created, {{{<x>}}} starting from 0.